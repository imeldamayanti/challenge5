# macOS
.DS_Store
**/.DS_Store

# Xcode / iOS
*.xcuserstate
**/*.xcuserstate
**/xcuserdata/
**/project.xcworkspace/xcuserdata/
**/xcworkspace/xcuserdata/
DerivedData/
**/DerivedData/
build/
**/build/

# SwiftPM / package manager
.build/
**/.build/
.swiftpm/
**/.swiftpm/
Package.resolved
**/Package.resolved

# Local editor / project metadata
.remember/
**/.remember/
.worktrees/
.superpowers/
.idea/
**/.idea/
.vscode/
**/.vscode/

# Supabase
.branches
.temp

# dotenvx
.env.keys
.env.local
.env.*.local

# Edge Function secrets (b3 §2). `supabase secrets set --env-file` reads these; none of them
# belongs in the repository, and the service_role key belongs in exactly two places — Edge Function
# environment and CI secrets (design §8).
.env
.env.production
